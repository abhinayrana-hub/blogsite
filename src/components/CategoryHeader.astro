---
interface Props {
  title: string;
  description: string;
  kicker?: string;
}

const { title, description, kicker = 'Journal' } = Astro.props;
---

<div class="category-header">
  <p class="kicker">{kicker}</p>
  <h1>{title}</h1>
  <p class="description">{description}</p>
</div>
